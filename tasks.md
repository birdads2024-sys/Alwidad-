# مهام المرحلة السابعة والثامنة: لوحة التحكم وتحسين تجربة المستخدم

- [x] **إنشاء المكونات المشتركة (Shared Widgets):**
  - [x] `lib/widgets/custom_text_field.dart`: حقل نصي أنيق ومتناسق مع الوضع المظلم.
  - [x] `lib/widgets/loading_widget.dart`: مؤشر تحميل بتصميم مخصص ومميز.
  - [x] `lib/widgets/empty_state_widget.dart`: واجهة حالة فارغة منسقة ومصورة للقوائم.
  - [x] `lib/widgets/error_widget.dart`: بطاقة عرض الخطأ مع زر إعادة المحاولة.
  - [x] `lib/widgets/subscription_banner.dart`: بانر احترافي لتشجيع الطلاب على الاشتراك.
  - [x] `lib/widgets/course_card.dart`: بطاقة عرض الدروس والفيديوهات للطلاب.
  - [x] `lib/widgets/student_card.dart`: بطاقة تفاصيل الطالب وعرض حالته وتصنيفه للإدارة.

- [x] **تتطوير واجهات لوحة التحكم (Admin Panel):**
  - [x] `lib/screens/admin/admin_dashboard_screen.dart`: واجهة لوحة التحكم الرئيسية للتنقل بين الأقسام وإدارة الطلاب والبيانات.
  - [x] `lib/screens/admin/pdf_management_screen.dart`: إدارة ملفات الـ PDF (إضافة، تعديل، حذف، وتصفية حسب الفصول الستة).
  - [x] `lib/screens/admin/questions_management_screen.dart`: إدارة الاختبارات والروابط (إضافة، تعديل، حذف، وتصفية حسب الفصول).
  - [x] `lib/screens/admin/settings_screen.dart`: إعدادات التطبيق (تغيير كلمة مرور المشرف، رابط فيديو اليوتيوب التعريفي، إدارة الروابط الإعلانية، رقم دعم واتساب، سياسة الخصوصية والاسترجاع).

- [x] **تحسين التصميم والواجهات (UI/UX Polish):**
  - [x] تعديل وتطوير `lib/config/app_theme.dart` لتحسين الألوان والخطوط والظلال لتناسب اللغة العربية والتوجيه من اليمين لليسار (RTL).
  - [x] دمج لوحة التحكم في شاشة تسجيل الدخول والشاشة الرئيسية لعرض المحتوى بناءً على دور المستخدم (Admin أو Student).

## مهام إضافة الجودات المتعددة للفيديو

- [x] **تعديل نموذج الكورس (`CourseModel`):**
  - [x] تعريف كلاس `VideoQuality` يحتوي على (اسم الجودة، رابط HLS، رابط MP4).
  - [x] إضافة قائمة `qualities` in `CourseModel` وتحديث `fromMap` و `toMap` و `toCacheMap` و `copyWith` مع الحفاظ على التوافق الرجعي.
- [x] **تطوير واجهة الإضافة والتعديل للأدمن (`AddEditCourseScreen`):**
  - [x] بناء واجهة تفاعلية ديناميكية لإضافة جودات متعددة وحذفها.
  - [x] تعديل التحقق من صحة البيانات (Validation) وطريقة حفظ الجودات في Firestore.
- [x] **تحديث واجهة عرض الفيديوهات للطلاب (`CourseVideosScreen`):**
  - [x] تعديل تصميم كارت الدرس لعرض الجودات المتاحة.
  - [x] جعل أزرار التشغيل والتحميل تعمل بشكل مستقل لكل جودة بناءً على روابطها.
- [x] **التشغيل والتحقق:**
  - [x] تشغيل التطبيق على Flutter Chrome والتحقق من عمل الميزات بشكل ممتاز.

